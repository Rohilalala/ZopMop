# ZopMop CRM — Verified Bugs (P0/P1)

> Adversarially verified 2026-06-14. 20 confirmed REAL, 7 refuted. Each confirmed bug carries a minimal surgical fix + files. Migration items are forward-only per repo policy.

## Confirmed — needs fix

### [P1] auth-rbac — `App/househelp-api/internal/crm/auth/service.go:164-241 (VerifyTOTPAndIssue)`
- **Problem evidence:** VerifyTOTPAndIssue (service.go:164-241) calls ParseChallengeToken then CreateSession and never records the challenge as consumed. ChallengeClaims (jwt.go:137-141) carries only AdminID + PendingTOTP — no jti — and IssueChallengeToken (jwt.go:144-159) sets only iat/exp, so the token is a pure stateless JWT replayable until exp (5 min). VerifyTOTP (totp.go:46-51) is a bare totp.Validate with no used-code/jti tracking, so a code is reusable across its ±1 30s window. crm_admin_sessions has UNIQUE only on refresh_token_hash (fresh per call) — no unique/index on admin_id (migration 039 line 44 + partial non-unique idx line 53-54; migration 072 adds family columns only), so each call INSERTs a new active session. CreateSession (repository.go:150-166) has no ON CONFLICT. Route is mounted at cmd/crm-api/main.go:401 (r.Post("/totp/verify", loginLimiter, ...) handler.go:51). The only rate guard is crmLoginLimiter default 5 req / 15 min per IP shared between /login and /totp/verify (crmconfig/config.go:89-90), which does NOT block 3 verify calls in 30s and is bypassable by IP rotation. No challenge-consumption/jti/used-code/nonce tracking exists anywhere in the auth package or migration 039. Mitigating context (severity only, not a refutation): crm-api is not the prod-deployed binary — railway.json startCommand runs /usr/local/bin/api (cmd/api), and repo CLAUDE.md states "CRM is not deployed to prod" — but the bug is scoped to the CRM and the code path fully reproduces.
- **Fix:** Make the challenge token single-use. Add a jti (uuid) claim to ChallengeClaims and stamp it in IssueChallengeToken. Add a forward-only migration creating crm_used_challenges (jti UUID PRIMARY KEY, admin_id, consumed_at TIMESTAMPTZ DEFAULT now(), expires_at). In VerifyTOTPAndIssue, after a successful TOTP check and BEFORE CreateSession, atomically INSERT the jti (INSERT ... ON CONFLICT (jti) DO NOTHING and check rows-affected, or rely on the PK unique violation); if the row already existed, return ErrInvalidChallenge (challenge already used) and do not create a session. Set expires_at to the challenge exp so rows self-expire (sweep with the existing expired-session cleanup). This caps each captured challenge+code to exactly one session and bounds the TOTP-reuse window. Keep audit logging on the rejected-replay path.
- **Files:** App/househelp-api/internal/crm/auth/jwt.go, App/househelp-api/internal/crm/auth/service.go, App/househelp-api/internal/crm/auth/repository.go, App/househelp-api/migrations/130_crm_used_challenges.up.sql

### [P1] dashboard — `/Users/adityarohilla/Documents/ZopMop/App/househelp-api/internal/crm/alerts/alerts.go:143`
- **Problem evidence:** alerts.go:142 gates GET /alerts with middleware.RequirePermission("alerts.read") but alerts.go:143 mounts r.Post("/alerts/read-all", h.MarkAllRead) with NO permission middleware. The group it mounts under is `authed := api.Group("", jwtMW, crmAdminLimiter)` (cmd/crm-api/main.go:410) — only JWT auth + rate limiter, no RBAC. So any authenticated admin including viewer (roleRank 0 in auth/permissions.go:11) reaches it. MarkAllRead (alerts.go:116-130) runs `UPDATE crm_alerts SET read_by = ...` with NO WHERE clause — global mutation; read_by is NOT NULL JSONB (migration 039_crm_core.up.sql:95). Audit gap confirmed: alertsHandler is built as alerts.NewHandler(alertsSvc) (crm-api/main.go:206) with NO auditRecorder, unlike every sibling write handler (users/workers/orders/flags all get auditRecorder, lines 203-318); the alerts package never imports audit. The group comment at crm-api/main.go:403-405 says "Every write below must call the audit recorder" — this route violates it. No DB constraint, no caller guard, no IsProduction gate mitigates. Idempotent CASE (alerts.go:121) limits damage but does not refute "viewer can mutate read_by on every alert row globally."
- **Fix:** Gate the write and audit it, matching sibling handlers. (1) Add a write permission key to internal/crm/auth/permissions.go, e.g. "alerts.mark_read": RoleSupport (or RoleViewer if read-state is deliberately viewer-writable — but it must be an explicit key, not unguarded). (2) In alerts.go RegisterRoutes line 143 chain middleware.RequirePermission("alerts.mark_read") before h.MarkAllRead. (3) Wire the audit recorder: change alerts.NewHandler to accept *audit.Recorder, pass auditRecorder at crm-api/main.go:206, and in MarkAllRead emit an audit.Entry (action "alerts.mark_all_read", module "crm", target_type "alert") on success so the mutation appears in crm_audit_log like every other CRM write.
- **Files:** /Users/adityarohilla/Documents/ZopMop/App/househelp-api/internal/crm/alerts/alerts.go, /Users/adityarohilla/Documents/ZopMop/App/househelp-api/internal/crm/auth/permissions.go, /Users/adityarohilla/Documents/ZopMop/App/househelp-api/cmd/crm-api/main.go

### [P1] analytics — `/Users/adityarohilla/Documents/ZopMop/App/househelp-api/internal/crm/analytics/analytics.go:131-140 vs 46-62`
- **Problem evidence:** Summary() line 136-139 sums completed revenue with `WHERE created_at >= $1 AND created_at <= $2`, while RevenueDaily() lines 56-58 sums completed revenue with `b.status='completed' AND b.completed_at >= d.day ... AND b.completed_at < ...` — two different columns. Both are distinct on bookings: created_at is stamped at insert (migration 004_create_bookings.up.sql:17 DEFAULT now()); completed_at is a separate nullable column added in 023_analytics_schema.up.sql:39 and set to NOW() only at completion (booking/service.go:1831 `status='completed', completed_at = NOW()`; jobs.go:462). No CHECK/trigger forces them into the same window. Both endpoints are mounted (cmd/crm-api/main.go:428 analyticsHandler.RegisterRoutes(authed) under app.Group("/admin")) and consumed on the SAME screen with the SAME from/to params: zopmop-crm/src/pages/AnalyticsPage.tsx line 46 ('Revenue' KPI ← summary.revenue_paise) and lines 54-62 ('Revenue / day' chart ← revenueDaily). For a booking created 40d ago, completed yesterday, the 30d chart includes its revenue but the Revenue card excludes it — the totals diverge. (Repo's own dashboard.go:164-165 uses completed_at, so Summary is the inconsistent one.)
- **Fix:** Align Summary()'s revenue computation to the same recognition basis as RevenueDaily (completed_at). Replace the single shared `WHERE created_at` predicate so that the SUM/AVG-over-completed aggregates are windowed on completed_at, not created_at — e.g. change lines 136-137 to `COALESCE(SUM(amount_paise) FILTER (WHERE status='completed' AND completed_at >= $1 AND completed_at <= $2),0)` and the AVG filter likewise, while leaving the Orders/Completed/Cancelled COUNTs on created_at (those are legitimately creation-based). Keep int64 paise (no floats). Read-only query, no audit/IsProduction concerns. Optionally document on AnalyticsPage that order-count cards are creation-based while revenue is completion-based so the two never silently appear to belong to the same basis.
- **Files:** /Users/adityarohilla/Documents/ZopMop/App/househelp-api/internal/crm/analytics/analytics.go

### [P1] orders — `/Users/adityarohilla/Documents/ZopMop/App/househelp-api/internal/crm/orders/orders.go:409-422`
- **Problem evidence:** MarkComplete (orders.go:410-414) runs `UPDATE bookings SET status='completed', completed_at=now(), updated_at=now() WHERE id=$1 AND status NOT IN ('completed','cancelled')` — the WHERE clause only excludes terminal states, so a 'pending'/'searching'/'dispatching' order with NO helper flips straight to completed. Route is mounted at orders.go:690 `g.Post("/:id/complete", RequirePermission("orders.complete"), h.Complete)` — an admin with that permission reaches it via direct POST; the SPA button-disable is client-only and the handler comment 'Pro Mode only on the SPA' (orders.go:817) has no server gate / IsProduction check. The status CHECK constraint in migrations/101_job_lifecycle.up.sql:35-51 is a value-set only (pending→completed passes), and the only bookings trigger (060/062) is BEFORE INSERT dedup — nothing enforces a transition or fires on UPDATE. NOTE: the bug's 'triggers downstream earnings' sub-claim is overstated — MarkComplete writes no pro_earnings_paise/customer_rating_pending/shift credit; those are set only by the pro-side CompleteBooking (service.go:1830-1868, which requires helper_id=$2 AND status='in_progress'). But the illegal, unguarded state transition itself is real: it corrupts order lifecycle/analytics (completed_at index 023, dashboard completed-counts) and bypasses dispatch/accept, with no mitigation.
- **Fix:** Add a source-status whitelist to the MarkComplete SQL so only genuinely completable orders can be forced complete, mirroring the SPA's enabled states: change WHERE to `WHERE id = $1::uuid AND status IN ('accepted','arrived','en_route','in_progress')` (use the canonical en-route status value used elsewhere). RowsAffected()==0 then correctly returns an error for pending/searching/dispatching/helper-less orders. The write is already audited at orders.go:823 (order.complete_manual); no IsProduction gate needed since this is a pure DB state change, but optionally also reject when helper_id IS NULL to guarantee no helper-less completion.
- **Files:** /Users/adityarohilla/Documents/ZopMop/App/househelp-api/internal/crm/orders/orders.go

### [P1] payouts-payroll — `App/househelp-api/internal/crm/payouts/payouts.go:128-141, 232-243`
- **Problem evidence:** Legacy MarkPaid (payouts.go:128-141) and MarkFailed (143-155) do a bare r.write.Exec UPDATE that commits immediately, with no transaction. The audit is written only afterward in the handler: h.audit(...) at payouts.go:211 (paid) and 228 (failed), which calls Recorder.Log. Recorder.Log (audit.go:48-91) returns no error and explicitly swallows the INSERT failure (audit.go:41-44 doc: "Failures are logged but never propagated"; 83-90 logs and returns). So if the audit INSERT fails or the process dies between the UPDATE commit and the audit INSERT, the money status flips to paid/failed with no guaranteed audit row. The payroll-engine module does the opposite: UPDATE + insertAudit(tx,...) in one tx and aborts the commit on audit error (payroll.go:243-260, "audit log: %w"). No mitigation exists: the LogTx referenced in audit.go:47 is not defined anywhere (grep returns only the comment); no DB trigger writes audit rows; a UNIQUE/CHECK/NOT NULL constraint on crm_payouts cannot produce a missing audit row. Route is live and reachable: payoutsHandler.RegisterRoutes(authed) at cmd/crm-api/main.go:432 with a real auditRecorder (main.go:265), guarded by RequirePermission("payouts.mark_paid"/"payouts.mark_failed").
- **Fix:** Make the audit write atomic with the status change, mirroring the payroll engine. Add a transactional audit API (e.g. Recorder.LogTx(ctx, tx, Entry) in internal/crm/audit/audit.go that runs the same INSERT against a pgx.Tx and returns its error), then rewrite Repository.MarkPaid and MarkFailed to BeginTx, run the UPDATE, call LogTx inside the same tx (propagating any error to abort), and Commit. Pass adminID/email/IP/UA/RequestID into the repo (as the payroll repo takes adminID) so the in-tx audit row matches the current handler payload. Remove the now-duplicate after-the-fact h.audit("payout.paid"/"payout.failed", ...) calls in the handler (payouts.go:211, 228) so only the transactional row is written. Keep int64 paise unchanged; this only touches the audit/transaction wiring.
- **Files:** App/househelp-api/internal/crm/payouts/payouts.go, App/househelp-api/internal/crm/audit/audit.go

### [P1] payouts-payroll — `App/househelp-api/internal/crm/payouts/payouts.go:128-141 (MarkPaid) & migration 041_crm_modules.up.sql crm_payouts`
- **Problem evidence:** migrations/041_crm_modules.up.sql:66-79 defines crm_payouts with NO actor column (only worker_id, period_*, amount_cents, status CHECK, paid_at, external_ref, notes, timestamps). payouts.go:129-133 MarkPaid UPDATE sets only status='paid', paid_at=now(), external_ref, updated_at — never an actor. Contrast: migration 110_payroll_admin_audit.up.sql:16-17 adds payouts.paid_by_admin_id UUID REFERENCES crm_admins(id), so the new engine records the actor at the row level. The only actor capture for the legacy path is the audit log (payouts.go:211 -> audit() at :232-243 reading crmAdminID), but audit/audit.go:41-48,83-90 is explicitly best-effort/fire-and-forget: 'Failures are logged but never propagated... a missing audit row is a P1 incident.' Route is mounted and reachable: payouts.go:170 g.Post('/:id/paid', RequirePermission('payouts.mark_paid')).
- **Fix:** Add a paid_by_admin_id UUID REFERENCES crm_admins(id) column to crm_payouts via a new forward-only migration (e.g. 0NN_crm_payouts_paid_by.up.sql, no .down.sql per repo policy), then thread the admin id from request locals into MarkPaid: change MarkPaid signature to accept paidByAdminID, set paid_by_admin_id=$3::uuid in the UPDATE, and pass c.Locals('crmAdminID') from the handler. Same for MarkFailed (failed_by_admin_id) for symmetry. Keep the existing audit() call. No floats touched (amount stays BIGINT paise).
- **Files:** App/househelp-api/internal/crm/payouts/payouts.go, App/househelp-api/migrations/0NN_crm_payouts_paid_by.up.sql

### [P0] workers — `App/househelp-api/internal/crm/workers/repository.go:235-247 (Get) and model.go:62-71; App/zopmop-crm/src/pages/workers/WorkerDrawer.tsx:528-585`
- **Problem evidence:** handler.go:43,46 mounts `g.Get("/:id", read, h.Get)` where `read = RequirePermission("workers.read")`; permissions.go:180 maps `"workers.read": RoleSupport`; permissions.go:195 `return roleR >= roleRank[min]` so support (rank 1 >= 1) passes. handler.go Get returns `c.JSON(d)` with no masking — repository.go SQL (lines 199-201) selects raw `h.aadhaar_number`, `h.bank_account_number` into d.AadhaarNumber/d.BankAccountNumber, and the TODO at repository.go:233-234 to null PII for non-superadmin is unimplemented; model.go:59-68 confirms the JSON fields ship raw with only TODO comments. Frontend WorkerDrawer.tsx:528-585 masks via client-only `showAadhaar`/`showBank` state on values already present in `worker.aadhaar_number`/`worker.bank_account_number`; the eye toggle is a pure React setState with no network call, so no reveal audit is written. handler.go Get only logs `worker.view` on page load. grep for redact/MaskPII/masking middleware across internal/crm returned no server-side masking. A direct API call or second browser tab with a support token reaches the raw plaintext PII.
- **Fix:** In repository.go Get, accept the caller role (thread crmAdminRole into Get, or null PII in the handler) and when role != superadmin set d.AadhaarNumber = nil and d.BankAccountNumber = nil before returning. Additionally, add a server-side reveal endpoint (e.g. GET /workers/:id/pii gated on a superadmin-only permission like workers.read_pii) that returns the unmasked values and writes an audit event (action worker.pii.reveal) via h.recorder; switch the WorkerDrawer eye toggle to call that endpoint instead of revealing pre-shipped data. Keep audit on every read of unmasked PII. (Encryption-at-rest with KMS is a follow-up; the immediate P0 fix is role-based nulling + reveal audit.)
- **Files:** App/househelp-api/internal/crm/workers/repository.go, App/househelp-api/internal/crm/workers/handler.go, App/househelp-api/internal/crm/auth/permissions.go, App/zopmop-crm/src/pages/workers/WorkerDrawer.tsx

### [P1] workers — `App/zopmop-crm/src/pages/workers/WorkerDrawer.tsx:642-649 & 692-716; App/househelp-api/internal/crm/workers/repository.go:773-783`
- **Problem evidence:** handler.go:262-269 ForceOffline calls h.repo.ForceOffline(id) directly with NO HasActiveJob check and never errors on an active job; the only RBAC gate is RequirePermission("workers.force_offline") at handler.go:54. repository.go:774-783 ForceOffline runs only `UPDATE helpers SET is_available = FALSE` — booking helper_id/status untouched (self-documented at repository.go:773 "Doesn't cancel any active booking"). matching/engine.go:213 filters `WHERE h.is_available = true`, so the worker is pulled from matching while the accepted/arrived/in_progress booking stays assigned to them (stranded). HasActiveJob (repository.go:353-371, exposed only via ActiveJob handler GET /active-job at handler.go:178-186) is purely informational. On the client, workerActiveJob is imported (WorkerDrawer.tsx:37) but used only in PerformanceTab (line 753-754); the force-offline ConfirmModal (lines 692-716, opened by setConfirm('offline') at line 645) shows only a static impact string and never queries the active job. No DB trigger cascades the booking on is_available change (grep of migrations found none).
- **Fix:** Guard force-offline against in-flight bookings on the server (authoritative): in ForceOffline handler call repo.HasActiveJob first; if it returns true, return 409 with the blocking booking_id instead of flipping is_available — or wrap the flip + booking cancel/reassign in a single transaction (cancel/unassign the accepted/arrived/in_progress booking, restore it to the dispatch queue) so no booking is stranded. Keep the write audited (h.audit already present) and add the booking_id to the audit metadata. On the client, before confirming force-offline, fetch workerActiveJob(worker.id) and surface the active-job warning in the ConfirmModal (and disable/route to reassign when has_active is true) to match the documented intent. No money paths touched; no migration required for the minimal 409-guard fix.
- **Files:** App/househelp-api/internal/crm/workers/handler.go, App/househelp-api/internal/crm/workers/repository.go, App/zopmop-crm/src/pages/workers/WorkerDrawer.tsx

### [P1] workers — `App/househelp-api/internal/crm/workers/repository.go:728-739 (Reject) and handler.go:216-227`
- **Problem evidence:** handler.go:217-222 parses RejectRequest, validates req.Reason non-empty (line 218), but calls h.repo.Reject(ctx, id) passing ONLY the id — reason is dropped. Line 225 routes reason solely into h.audit("worker.reject", id, nil, req.Reason). repository.go:728-739 Reject(ctx, workerID string) has no reason param and runs only `UPDATE helpers SET approval_status='rejected'`. The repo comment (726-727) falsely claims "Reason is stored as a note" but grep shows the workers Repository has NO crm_user_notes/AddNote function (the only note-writer is internal/crm/users/repository.go) and Reject never calls across to it. No helpers column for it: migrations 033/106/111 add approval_status, KYC/payment fields, flags — no rejection_reason. migration 111:50 review_notes belongs to the separate helper_flags table (CREATE TABLE at 111, status CHECK 45-46), not helpers. Contrast with Suspend (repository.go:744-756) which DOES persist suspend_reason. Route mounted at handler.go:51 behind RequirePermission("workers.reject") — reachable by admin; reason flows in but persistence sink does not exist.
- **Fix:** Persist the rejection reason on the worker record, mirroring the existing Suspend pattern. (1) Forward-only migration adding `rejection_reason TEXT` to helpers. (2) Change repository.go Reject signature to Reject(ctx, workerID, reason string) and SQL to `UPDATE helpers SET approval_status='rejected', rejection_reason=$2 WHERE id=$1::uuid` (keep RowsAffected==0 -> ErrNotFound). (3) handler.go:222 pass req.Reason: h.repo.Reject(c.UserContext(), id, req.Reason). Leave the existing h.audit call intact. Optionally clear rejection_reason on Approve and surface it in Get/List. Fix the misleading comment at repository.go:726-727.
- **Files:** App/househelp-api/internal/crm/workers/repository.go, App/househelp-api/internal/crm/workers/handler.go, App/househelp-api/migrations/NNN_helpers_rejection_reason.up.sql

### [P1] workers — `App/househelp-api/internal/crm/workers/repository.go:708-724 (Approve)`
- **Problem evidence:** repository.go:709-716 — Approve runs `UPDATE helpers SET approval_status='approved' WHERE id=$1::uuid` with NO state filter (no `AND approval_status='pending'`). migrations/033_helpers_pending_status.up.sql:1 declares approval_status as `VARCHAR(20) NOT NULL DEFAULT 'pending'` with NO CHECK constraint, and a grep for any CHECK on approval_status across migrations/ returns NONE (other columns like gender at 106:38 and helper_flags.status at 111:46 DO have CHECK enums — this one deliberately doesn't). The route POST /workers/:id/approve is mounted at cmd/crm-api/main.go:422 on the authed admin group behind only RequirePermission("workers.approve") (handler.go:50) — no per-state guard. RowsAffected()==0→ErrNotFound (repository.go:720) only catches a missing id, not an illegal transition, so a 'rejected' or already-'active' worker is silently re-approved with 200. Approve also never resets is_suspended/banned on the user row (those flags live in users; Approve only sets role='pro'). The only nuance vs the report: the handler DOES audit the action (handler.go:204 h.audit "worker.approve"), but the audit does not record the prior state and does not prevent the illegal transition.
- **Fix:** Add a state-machine guard to the Approve repository SQL so only pending applications can be approved: change the CTE to `UPDATE helpers SET approval_status='approved' WHERE id=$1::uuid AND approval_status='pending' RETURNING id`. When RowsAffected()==0, distinguish missing-id from illegal-state by a follow-up existence check (or return a new ErrInvalidState mapped to 409 Conflict in handler.errResp). Apply the same guard to Reject (only allow from 'pending'). Defense-in-depth: add a forward-only migration with a CHECK constraint `approval_status IN ('pending','approved','rejected')` to lock the enum. Keep the existing h.audit call; optionally include prior state in the audit payload. No money/float concerns; the existing audit write satisfies the audit-every-write rule.
- **Files:** App/househelp-api/internal/crm/workers/repository.go, App/househelp-api/internal/crm/workers/handler.go, App/househelp-api/migrations/112_helpers_approval_status_check.up.sql

### [P1] zone-approvals — `/Users/adityarohilla/Documents/ZopMop/App/househelp-api/internal/crm/zoneapprovals/handler.go:67-88 (Reject) — only NULLIF in repository.go:480`
- **Problem evidence:** Backend chain has zero length validation on the rejection reason. handler.go:72-89 Reject: `var req rejectRequest; _ = c.BodyParser(&req); h.svc.RejectZoneRequest(ctx, id, adminID, req.Notes)` — rejectRequest (handler.go:67-69) has no validation tags. service.go:361-365 RejectZoneRequest passes `notes` straight to repo. repository.go:472-482 DecideZoneApproval stores `notes = NULLIF($4,'')` (empty→NULL, 1-char stored verbatim). migrations/098_shift_system.up.sql:136-148 defines `notes TEXT` with no NOT NULL/CHECK/length constraint (only constraint on table is status CHECK). The smoke test zoneapproval_smoke_test.go:119 even passes "x" and asserts success. The ONLY min-length guard is client-side React: ZoneApprovalsPage.tsx:265 `disabled={!canReject || notes.trim().length < 5}`. Route is reachable: cmd/crm-api/main.go:437 mounts on `authed` group (JWT+limiter, main.go:410); handler.go:35 gates with RequirePermission("zones.approval.reject"). A direct API POST /admin/zone-approvals/<id>/reject {"notes":""} by a permitted admin returns 200 "rejected" with DB notes=NULL, producing an audit-logged rejection with no auditable reason.
- **Fix:** Add server-side min-length validation in zoneapprovals.Reject before calling the service: trim req.Notes and reject with 400 if len < 5 (e.g. `if len(strings.TrimSpace(req.Notes)) < 5 { return c.Status(400).JSON(fiber.Map{"error":"notes required (min 5 chars)"}) }`). Mirror the existing client rule. Keep audit-after-success behavior unchanged; no migration needed (validation belongs at the handler since NULLIF intentionally allows NULL for the approve path).
- **Files:** /Users/adityarohilla/Documents/ZopMop/App/househelp-api/internal/crm/zoneapprovals/handler.go

### [P1] disputes — `/Users/adityarohilla/Documents/ZopMop/App/househelp-api/internal/crm/trustsafety/trustsafety.go:142-154`
- **Problem evidence:** trustsafety.go:143-146 ResolveDispute runs `UPDATE crm_disputes SET status='resolved', resolution=$2, resolved_at=now(), updated_at=now() WHERE id = $1::uuid` with NO status guard, vs the sibling SetDisputeStatus at line 166 which has `AND status NOT IN ('resolved','closed')`. The only DB constraint on status (migration 041_crm_modules.up.sql:96-97) is an enum-membership CHECK (open/in_progress/resolved/escalated/closed) — no trigger or state-machine constraint; migration 081 only adds a `reason` column. Route POST /admin/disputes/:id/resolve is mounted (cmd/crm-api/main.go:434 -> trustsafety.go:340) under the JWT-authed group, gated by RequirePermission("disputes.resolve") which auth/permissions.go:85 grants to RoleSupport. On re-resolve, the row still exists so RowsAffected()==1 -> handler returns 200 {ok:true} (trustsafety.go:390), resolution/resolved_at are overwritten, and audit.Log (audit.go:48-82) does an unconditional INSERT, writing a second dispute.resolve audit row. No reopen path exists, so the net effect is silent rewrite of a closed case.
- **Fix:** Add the same state guard the sibling uses: change the ResolveDispute UPDATE WHERE clause to `WHERE id = $1::uuid AND status NOT IN ('resolved','closed')`. Then RowsAffected()==0 on an already-resolved/closed dispute returns the existing ErrNotFound -> 404, preventing the overwrite and the spurious second audit row. No migration needed (forward-only policy preserved); single-line surgical change matching existing style.
- **Files:** /Users/adityarohilla/Documents/ZopMop/App/househelp-api/internal/crm/trustsafety/trustsafety.go

### [P1] promos — `/Users/adityarohilla/Documents/ZopMop/App/househelp-api/internal/crm/promos/promos.go:232-255`
- **Problem evidence:** Create() guards future expiry at promos.go:203-205 (`if req.ExpiresAt != nil && req.ExpiresAt.Before(time.Now()) { return ...expires_at must be in the future }`). Update() (promos.go:232-255) only calls validateCreateRequest (code + discount bounds) then runs the UPDATE; it never re-checks expires_at, so a past date is written and the handler returns c.JSON({ok:true}) = HTTP 200 (Update handler promos.go:416-427). No DB-level mitigation: migration 007_create_promotions.up.sql declares expires_at TIMESTAMPTZ with no CHECK; the only promotions constraints added later are discount_value bounds (109) and audience (092) — neither touches expires_at. Route PUT /promos/:id is mounted behind RequirePermission("promos.update") (promos.go:345; middleware exists at internal/crm/middleware/jwt.go:124), so any admin with promos.update reaches it directly. The booking-side ValidatePromoCode (service.go:1346) correctly rejects expired promos — which is precisely what makes the edited promo silently dead rather than dangerous, confirming the stated P1 impact (data-integrity/UX, not money loss) and that nothing prevents it.
- **Fix:** In Repository.Update (promos.go, after the validateCreateRequest call at line 233-235), mirror Create's guard: `if req.ExpiresAt != nil && req.ExpiresAt.Before(time.Now()) { return fmt.Errorf("expires_at must be in the future") }`. The handler already maps repo errors to HTTP 400 (promos.go:423), so this turns the silent 200 into a proper 400. No migration needed; no money/audit semantics change (audit already fires at promos.go:425).
- **Files:** /Users/adityarohilla/Documents/ZopMop/App/househelp-api/internal/crm/promos/promos.go

### [P0] promos — `/Users/adityarohilla/Documents/ZopMop/App/househelp-api/internal/crm/promos/promos.go:156-197`
- **Problem evidence:** validateCreateRequest (promos.go:180-197) only checks Code presence and discount_value bounds; min_order_cents, max_uses, max_per_user are never validated and flow straight into the INSERT (promos.go:212-224) and UPDATE (238-247). No DB CHECK guards them: migration 007 defines min_order_cents/max_uses as `INTEGER NOT NULL DEFAULT 0` (007:9-10), migration 041 defines max_per_user as `INT NOT NULL DEFAULT 0` (041:15) — both with no CHECK; migration 109 only constrains discount_value. Postgres INTEGER accepts negatives. Route POST /promos is reachable by admin via RequirePermission("promos.create") (promos.go:342) and BodyParser maps the JSON fields directly. Booking-time impact is real for max_uses: both ValidatePromoCode (booking/service.go:1351 `promo.MaxUses > 0 && promo.UsesCount >= promo.MaxUses`) and IncrementPromoCodeUsage (booking/repository.go:913 `maxUses > 0 && usesCount >= maxUses`) treat a negative max_uses as "no cap" (the `> 0` short-circuits false), so a negative value silently disables the total-redemption limit. min_order_cents<0 nullifies the minimum-order floor (service.go:1356). The doc comment at promos.go:156-157 falsely claims "basic non-negativity." NOTE: the max_per_user part of the report is overstated — max_per_user is never enforced at booking time (no reference anywhere in internal/booking/), only projected in offers/handler.go:70; so it is display-only and a negative value has no money effect. The admin DTO has `validate:"min=0,max=1000000"` (admin/model.go:120) but that guards a different legacy handler, not this CRM path.
- **Fix:** Add non-negativity validation in validateCreateRequest (promos.go) before the INSERT/UPDATE: reject min_order_cents < 0, max_uses < 0, and max_per_user < 0 (returns HTTP 400). For defense-in-depth and to match the discount_value precedent, add a forward-only migration with a CHECK constraint: `ALTER TABLE promotions ADD CONSTRAINT promotions_caps_nonneg CHECK (min_order_cents >= 0 AND max_uses >= 0 AND max_per_user >= 0);` Update the misleading doc comment at promos.go:156-157. (No floats — these are integer paise/counts already; no IsProduction gate needed; audit already fires via h.audit on create/update.)
- **Files:** /Users/adityarohilla/Documents/ZopMop/App/househelp-api/internal/crm/promos/promos.go, /Users/adityarohilla/Documents/ZopMop/App/househelp-api/migrations/110_promo_caps_nonneg.up.sql

### [P1] promos — `/Users/adityarohilla/Documents/ZopMop/App/zopmop-crm/src/pages/PromosPage.tsx:16-86`
- **Problem evidence:** PromosPage.tsx:16 inits params={status,search,limit:50,offset:0}; offset is only ever reset to 0 on search/status change (lines 40-41) and never incremented — no next/prev/page UI exists anywhere in the render (lines 26-86), and total_count is never read. Backend genuinely paginates: promos.go:340 `g.Get("/", read, h.List)` (RequirePermission promos.read), handler reads offset via `strconv.Atoi(c.Query("offset","0"))` (promos.go:352) and returns `total_count` (promos.go:358); Repository.List (promos.go:71-130) applies `LIMIT $.. OFFSET $..` ORDER BY created_at DESC with default limit 50 / max 200. So with >50 promotions, rows beyond the first page are returned by the API only when offset is sent, which the frontend never does — they are unreachable except by searching by code. No mitigation: backend supports it, frontend just never drives offset. (Note: CRM is not prod-deployed per repo CLAUDE.md, but this is a genuine CRM functional gap.)
- **Fix:** Add pagination controls to PromosPage: render Prev/Next buttons (or page numbers) below the table, disable Prev when offset===0 and Next when offset+limit>=q.data.total_count, and wire them to setParams((p)=>({...p, offset: p.offset±p.limit})). Optionally show "X–Y of total_count". No backend change needed — limit/offset/total_count are already supported.
- **Files:** /Users/adityarohilla/Documents/ZopMop/App/zopmop-crm/src/pages/PromosPage.tsx

### [P1] banners — `/Users/adityarohilla/Documents/ZopMop/App/househelp-api/internal/crm/banners/banners.go:183-197 (Reorder) and /Users/adityarohilla/Documents/ZopMop/App/zopmop-crm/src/pages/BannersPage.tsx:32-39 (move)`
- **Problem evidence:** banners.go:189-191 loops `UPDATE banners SET display_order=$2 WHERE id=$1` only for the ids passed — no completeness check (no len(ids) vs COUNT, no SELECT id FROM banners), no row lock / advisory lock / FOR UPDATE (grep returned nothing). Migration 006_create_banners.up.sql:10 declares `display_order INTEGER NOT NULL DEFAULT 0` with only a NON-unique composite index idx_banners_active_schedule (lines 20-22); no later migration adds a UNIQUE constraint on display_order. So POST /banners/reorder {"ids":["<oneId>"]} sets that row to 0 and leaves all others untouched → duplicate display_order. List() (banners.go:69) ORDERs BY display_order ASC, created_at DESC, so tied rows become non-deterministic. Route is mounted/reachable (cmd/crm-api/main.go:426, perm banners.reorder). Frontend move() (BannersPage.tsx:32-39) calls reorder.mutate on every click; arrow buttons (lines 81,93) disable only on i===0 / i===length-1 / !canReorder — no reorder.isPending guard, so double-clicks or two stale tabs fire overlapping racing POSTs (last-writer-wins). Audit IS recorded (banners.go:285), but ordering integrity is not protected.
- **Fix:** Backend: make Reorder idempotent and atomic — inside the existing tx, take pg_advisory_xact_lock(<banners constant>) (or SELECT id FROM banners FOR UPDATE) to serialize concurrent reorders, then load the full set of banner ids and require the submitted ids to exactly match (same count + same membership); reject partial/stale lists with a 400 before applying any UPDATE. Only after validation apply display_order=index for the complete ordered set. This guarantees a contiguous, duplicate-free ordering and makes a second concurrent/stale request a no-op or rejection rather than a corrupting last-writer-wins. Frontend: add `disabled={... || reorder.isPending}` to both arrow buttons (and early-return in move() if reorder.isPending) so rapid clicks / a single tab cannot fire overlapping reorder POSTs. Keep the existing audit log call.
- **Files:** /Users/adityarohilla/Documents/ZopMop/App/househelp-api/internal/crm/banners/banners.go, /Users/adityarohilla/Documents/ZopMop/App/zopmop-crm/src/pages/BannersPage.tsx

### [P1] push — `/Users/adityarohilla/Documents/ZopMop/App/househelp-api/internal/crm/growth/growth.go:32-44, 135-153, 211-226`
- **Problem evidence:** Go PushMsg struct (growth.go:32-44) has NO fields for sent_count/delivered_count/failed_count/error_message. ListPush SELECT (growth.go:136-138) and GetPush SELECT (growth.go:214-216) only select 11 columns ending at created_at — none of the four stat columns. Yet migration 044 adds sent_count/delivered_count/failed_count (INTEGER NOT NULL DEFAULT 0) and 045 adds error_message (TEXT), and SendPush WRITES them ('SET status=$2, sent_at=now(), sent_count=$3, delivered_count=$4, failed_count=$5' growth.go:316-321, and 'SET status=failed, error_message=$2' growth.go:261-263). Handler ListPush (growth.go:716-720) returns the struct verbatim as {items: out} with no enrichment. Frontend all.ts:234-237 declares these as optional fields; PushPage.tsx:260-262 gates showCounts on 'm.sent_count !== undefined || m.delivered_count !== undefined || m.failed_count !== undefined' and PushPage.tsx:266 gates errorText on 'm.status===failed && m.error_message'. Since the API JSON never contains those keys, both are always falsy: the 'Sent: x · Delivered: y · Failed: z' line (lines 280-284) and the red error text (lines 273-277) never render. No mitigation: no handler enrichment, no JSON tags exist for these columns in the struct, columns are confirmed in DB.
- **Fix:** Add the four fields to the Go PushMsg struct with JSON tags (SentCount int `json:"sent_count"`, DeliveredCount int `json:"delivered_count"`, FailedCount int `json:"failed_count"`, ErrorMessage *string `json:"error_message,omitempty"`), then add `sent_count, delivered_count, failed_count, error_message` to both the ListPush SELECT (growth.go:136-138) and GetPush SELECT (growth.go:214-216), and append the matching &m.SentCount, &m.DeliveredCount, &m.FailedCount, &m.ErrorMessage to both rows.Scan/QueryRow.Scan arg lists. The three counts scan into int (NOT NULL DEFAULT 0); error_message must scan into *string (nullable TEXT). No migration needed — columns already exist.
- **Files:** /Users/adityarohilla/Documents/ZopMop/App/househelp-api/internal/crm/growth/growth.go

### [P1] experiments-flags — `App/househelp-api/internal/crm/experiments/experiments.go:159-181 (SetStatus)`
- **Problem evidence:** experiments.go:159-181 SetStatus builds `UPDATE crm_experiments SET status = $2 ... WHERE id = $1::uuid` with no predicate on the current status and no from->to validation; the switch only conditionally sets started_at (running) or ended_at/winner (completed|rolled_out), so a draft->rolled_out call leaves started_at NULL while ended_at is set. Migration 041_crm_modules.up.sql:37-38 only CHECKs status IN (draft,running,paused,completed,rolled_out) — value enum, NOT transition path; idx at line 47 is non-unique. Route is mounted: cmd/crm-api/main.go:427 expHandler.RegisterRoutes(authed) -> experiments.go:201 g.Post("/:id/rollout", RequirePermission("experiments.rollout")...). permissions.go:68 maps experiments.rollout->RoleAdmin, so an admin (or a stale tab/direct API call) reaches it; RBAC gates WHO not WHICH transition. No row lock/CAS/guard anywhere in handler or repo. Repro in howToVerify is exact.
- **Fix:** Add a state-machine transition guard in SetStatus. Encode allowed transitions (draft->running, running->paused|completed|rolled_out, paused->running|completed|rolled_out, etc.) and enforce by adding a current-status predicate to the UPDATE: append `AND status = ANY($N)` (the set of valid source states for the requested target) to the WHERE clause so an invalid transition affects 0 rows; map RowsAffected()==0 (when the row exists) to a 409/400 "invalid transition" rather than ErrNotFound (do a Get first, or split the distinction by checking existence). This keeps it forward-only with no migration needed; no money/floats involved, and existing audit logging on the handlers is preserved.
- **Files:** App/househelp-api/internal/crm/experiments/experiments.go

### [P1] experiments-flags — `App/househelp-api/internal/crm/experiments/experiments.go:242-255 (Rollout)`
- **Problem evidence:** experiments.go:246 — Rollout only rejects empty winner (`body.Winner == ""`); no check that it is a member of the experiment's variant ids. It passes body.Winner straight to SetStatus (line 250). SetStatus (159-181) builds `winner_variant = $N` with no validation and never even reads the variants JSONB. Migration 041_crm_modules.up.sql:41 declares `winner_variant TEXT` with NO CHECK, NO FK, NO relation to the `variants` JSONB column (line 34) — so the DB cannot reject a bogus value. Route is mounted (cmd/crm-api/main.go:427 expHandler.RegisterRoutes(authed)) and gated only by RequirePermission("experiments.rollout")=RoleAdmin (auth/permissions.go:68), which authorizes the action but does not validate the payload value. The only `range Variants` in the package is the traffic-sum check in validate() (line 75), unrelated to winner. No server-side double validation, no DB constraint, no state/CAS guard — POST /admin/experiments/:id/rollout {winner:"does-not-exist"} is accepted and persisted.
- **Fix:** In Rollout (handler) load the experiment via h.repo.Get(ctx, id), then verify body.Winner matches one of exp.Variants[i].ID; return 400 ("winner is not a valid variant id") if not, before calling SetStatus. Equivalently, push the check into SetStatus by reading variants in the same write txn for status=="rolled_out". Keep the existing audit.Log call. No money/migration concerns; forward-only DB CHECK is impractical (winner refers into JSONB), so enforce in Go.
- **Files:** App/househelp-api/internal/crm/experiments/experiments.go

### [P1] sdui — `App/househelp-api/internal/bff/repository.go:218-251 (CreateDraft) vs :613-634 (InsertAllowedAction)`
- **Problem evidence:** Constraint UNIQUE(page_id, version, env) exists at migrations/035_sdui_tables.up.sql:27, and the row version='static-1.0' is seeded at migrations/036_seed_sdui_home.up.sql:25, so the collision is reachable. CreateDraft (repository.go:229-243) INSERTs via QueryRow→scanConfig; on a 23505 the RETURNING yields no row so scanConfig's row.Scan returns *pgconn.PgError, which is NOT pgx.ErrNoRows so scanConfig (repository.go:122-127) returns the raw err, then CreateDraft wraps it as fmt.Errorf("create draft: %w", err) preserving 'duplicate key value violates unique constraint'. The handler createDraft (admin_handler.go:166-169) hardcodes c.Status(500).JSON({"error": err.Error()}) with NO errors.Is(err, ErrConflict) branch. By contrast InsertAllowedAction (repository.go:627-630) maps 23505→ErrConflict and createAllowed (admin_handler.go:487-489) returns 409. No pre-insert existence guard exists in the create path (GetByVersion calls at admin_handler.go:178/250 belong to getConfig/patchDraft). Route POST /pages/:page_id/configs is mounted as write(a.createDraft) (admin_handler.go:74), reachable by any write-level admin. Confirmed: duplicate-version draft returns HTTP 500 leaking the raw pg constraint string instead of a clean 409.
- **Fix:** Mirror the existing InsertAllowedAction pattern. (1) In repository.go CreateDraft, after scanConfig's error (lines 240-243), translate a Postgres 23505 unique-violation to ErrConflict: `var pgErr *pgconn.PgError; if errors.As(err, &pgErr) && pgErr.Code == "23505" { return nil, ErrConflict }` before the generic fmt.Errorf wrap (errors + pgconn are already imported, used at line 627). (2) In admin_handler.go createDraft, before the 500 fallback (lines 167-169), add `if errors.Is(err, ErrConflict) { return c.Status(409).JSON(fiber.Map{"error": "a draft with this version already exists for this env"}) }`, matching createAllowed at line 487 (errors already imported). No migration, money, or audit changes needed — the failed insert correctly never reaches AppendAuditLog.
- **Files:** App/househelp-api/internal/bff/repository.go, App/househelp-api/internal/bff/admin_handler.go

## Refuted — no action

### [P1] auth-rbac — `App/househelp-api/pkg/crmconfig/config.go:86 (RefreshCookieSecure default true) + internal/crm/auth/handler.go:255-266 (setRefreshCookie)`
- **Why not a bug:** The env var CRM_REFRESH_COOKIE_SECURE (getEnvBool default true, config.go:86/199-205) is itself the designed, first-class per-environment override. Secure-by-default is the intended/correct secure posture for production HTTPS; flipping it to false for local plain-HTTP QA is the sanctioned configuration path, not a code gap. There is no security/correctness/state/money impact on any deployed or reachable path (CRM is not in prod; prod is HTTPS). This is a configuration/DX gotcha, not a backend defect.

### [P1] analytics — `/Users/adityarohilla/Documents/ZopMop/App/zopmop-crm/src/pages/AnalyticsPage.tsx:23-27,44-104`
- **Why not a bug:** Global axios response interceptor (src/api/client.ts:170-172) calls showToast on every non-401 error, including the analytics 500 which returns {"error":"internal error"} (analytics.go:230) and does not match the gateway_error short-circuit. ToastHost (mounted in main.tsx:25) renders a visible error toast, so the server error is NOT silent — it surfaces a toast to the operator, refuting the stated "no toast / silently misleading" impact. The remaining gap (skeleton-vs-error visual and no per-page retry button) is a minor UX-polish issue, not the P1 defect as framed.

### [P0] orders — `/Users/adityarohilla/Documents/ZopMop/App/househelp-api/internal/crm/refunds/refunds.go:1037-1046`
- **Why not a bug:** Partial UNIQUE index uq_pending_refunds_booking_active on pending_refunds(booking_id) WHERE status IN ('pending','approved','processed','processed_manual') (migrations/046_refund_gateway.up.sql:48-50). Both racing CreateFromOrder inserts write status='pending' with a non-null booking_id, so the DB rejects the second with a 23505 unique violation before runGateway is ever reached — no double pending row, no double refund.

### [P1] disputes — `/Users/adityarohilla/Documents/ZopMop/App/zopmop-crm/src/pages/DisputesPage.tsx:121-130`
- **Why not a bug:** ConfirmModal's internal `busy` state (Modal.tsx:93, set synchronously at line 101 before `await onConfirm()` and gating both `if (busy) return` at line 101 and `disabled={... || busy ...}` at line 141) fully prevents the double-fire. Since onConfirm returns the m.mutateAsync() promise, busy stays true for the entire in-flight POST, so a double-click on the "Resolve" confirm button produces exactly one POST and one dispute.resolve audit row.

### [P1] push — `/Users/adityarohilla/Documents/ZopMop/App/zopmop-crm/src/pages/PushPage.tsx:98-130`
- **Why not a bug:** Global axios response interceptor (src/api/client.ts:170-173) shows an error toast with the server's message/error for every non-401 response, plus precise RBAC handling for insufficient_permissions 403 (client.ts:152-160). All push API calls go through this `api` instance (src/api/all.ts:268-273). Additionally ConfirmModal.go() (src/components/ui/Modal.tsx:100-118) catches the mutateAsync rejection, keeps the modal open for correction/retry, and shows a fallback toast if the payload had no message/error.

### [P1] push — `/Users/adityarohilla/Documents/ZopMop/App/househelp-api/internal/notification/service.go:40-59, 115-122, 198-206; /Users/adityarohilla/Documents/ZopMop/App/househelp-api/internal/crm/growth/growth.go:233-333`
- **Why not a bug:** Established codebase-wide convention: push side effects are gated by FCM credential presence (fcmClient==nil → "mocked (FCM offline)", service.go:116/205/539/613), NOT IsProduction. The repo non-negotiables (root CLAUDE.md:16, App/CLAUDE.md landmine) intentionally scope IsProduction to OTP/SMS/payment/webhook and exclude push; the PR #27 incident was SMS. The production binary cmd/api uses the identical no-IsProduction push path, confirming this is intended, not an oversight. The CRM send route is additionally RBAC-gated (RequirePermission("push.send"), growth.go:704) and cmd/crm-api is not a prod-deployed binary per repo policy.

### [P1] localities-maps — `/Users/adityarohilla/Documents/ZopMop/App/househelp-api/internal/crm/zones/zones.go:24-41,107-110,162-172`
- **Why not a bug:** Architectural separation: crm_surge_rules.multiplier is never wired into any pricing calculation (no consumer outside zones.go). The booking pricing uses an independent config_manager surge multiplier from the app_config table, not the CRM zone surge rule. Additionally, per repo CLAUDE.md the CRM is not deployed to prod and must not be referenced from backend prod paths, so this module is not in the production charge path at all. Lat/Lon/RadiusKM are geographic floats, not money.
